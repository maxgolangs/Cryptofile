# 🔐 CryptoFile

**CryptoFile by @MaxGolang** — современное приложение для безопасного шифрования файлов и директорий с графическим интерфейсом.

## ✨ Возможности

- 🔒 **Шифрование файлов и директорий** — безопасное шифрование отдельных файлов или целых папок
- 🔓 **Расшифровка** — быстрое восстановление зашифрованных данных
- 📦 **Пакетная обработка** — рекурсивное шифрование/расшифровка всех файлов в директории
- 🖱️ **Drag & Drop** — перетаскивайте файлы и папки прямо в окно приложения
- 🎨 **Современный интерфейс** — красивый GUI на базе Fyne
- 🔐 **Безопасность** — использование ChaCha20-Poly1305 и Argon2
- 🪟 **Кросс-платформенность** — поддержка Linux и Windows

## 🛠️ Требования

- **Go 1.20** или выше
- Для сборки Windows версии на Linux: **MinGW-w64** (gcc-mingw-w64)

## 📦 Установка зависимостей

### Linux

```bash
go mod download
```

### Windows (для сборки Windows версии на Linux)

```bash
sudo apt install gcc-mingw-w64
```

## 🔨 Сборка

### Автоматическая сборка (Linux и Windows)

Запустите скрипт сборки:

```bash
chmod +x build.sh
./build.sh
```

Собранные бинарники будут в папке `dist/`:
- `dist/cryptofile-linux-amd64` — для Linux
- `dist/cryptofile-windows-amd64.exe` — для Windows

### Ручная сборка

#### Linux

```bash
go build -o cryptofile ./cmd/cryptor
```

#### Windows

На Windows:
```bash
go build -o cryptofile.exe ./cmd/cryptor
```

На Linux (кросс-компиляция):
```bash
CC=x86_64-w64-mingw32-gcc CGO_ENABLED=1 GOOS=windows GOARCH=amd64 go build -o cryptofile.exe ./cmd/cryptor
```

## 🚀 Запуск

### Linux

```bash
./dist/cryptofile-linux-amd64
```

или если собрали вручную:

```bash
./cryptofile
```

### Windows

Двойной клик на `cryptofile-windows-amd64.exe` или запуск из командной строки:

```cmd
cryptofile-windows-amd64.exe
```

## 📖 Использование

### Основные операции

1. **Зашифровать файл или директорию**
   - Нажмите кнопку "Зашифровать файл или директорию"
   - Выберите файл/папку через диалог или перетащите их в окно
   - Введите пароль (минимум 6 символов)
   - Выберите, удалять ли оригинал после шифрования

2. **Расшифровать файл**
   - Нажмите кнопку "Расшифровать файл"
   - Выберите зашифрованный файл (.encrypted) или перетащите его
   - Введите пароль
   - Выберите, удалять ли зашифрованный файл после расшифровки

3. **Пакетное шифрование**
   - Нажмите "Зашифровать директорию (рекурсивно)"
   - Выберите директорию
   - Все файлы в директории и подпапках будут зашифрованы

4. **Пакетная расшифровка**
   - Нажмите "Расшифровать директорию (рекурсивно)"
   - Выберите директорию с зашифрованными файлами
   - Все .encrypted файлы будут расшифрованы

### Перетаскивание файлов (Drag & Drop)

Просто перетащите файл или папку в окно приложения — путь автоматически заполнится!

## 🔒 Безопасность

### Алгоритмы и методы

- **Алгоритм шифрования:** ChaCha20-Poly1305 (AEAD)
- **Обработка пароля:** 
  1. Кодирование пароля в Base64
  2. Хеширование через SHA256 (32 байта)
  3. Вывод ключа через Argon2id с параметрами:
     - Time Cost: 4 итерации
     - Memory Cost: 128 MB
     - Parallelism: 4 потока
     - Hash Length: 32 байта
- **Безопасное удаление:** оригинальные файлы перезаписываются случайными данными перед удалением
- **Формат:** собственный формат с проверкой целостности

### Процесс шифрования пароля

```
Введенный пароль
    ↓
Base64 кодирование
    ↓
SHA256 хеширование (32 байта)
    ↓
Argon2id (с уникальной солью)
    ↓
Ключ шифрования (32 байта)
    ↓
ChaCha20-Poly1305 шифрование
```

## 📁 Структура проекта

```
cryptor/
├── cmd/
│   └── cryptor/          # Точка входа приложения
├── constants/            # Константы и параметры
├── internal/
│   ├── crypto/           # Криптографические функции
│   ├── file/             # Файловые операции
│   ├── security/         # Функции безопасности
│   └── obfuscation/      # Обфускация
├── pkg/
│   ├── encrypt/          # Пакет шифрования
│   ├── decrypt/          # Пакет расшифровки
│   └── batch/            # Пакетная обработка
├── ui/                   # Пользовательский интерфейс
├── dist/                 # Собранные бинарники (создается при сборке)
├── build.sh              # Скрипт сборки
└── go.mod                # Зависимости Go
```

## 🤝 Поддержка

Автор: **@MaxGolang**

## ⚠️ Важно

### Требования к паролю

- Минимум **6 символов**
- Рекомендуется использовать сложные пароли с буквами, цифрами и символами
- Пароль невозможно восстановить — сохраните его в безопасном месте

### Безопасное удаление

При включенной опции "Удалить оригинал", файлы **безвозвратно удаляются** после безопасной перезаписи.  
Этот процесс нельзя отменить, убедитесь, что у вас есть резервная копия.

## 📄 Лицензия

Используйте на свой страх и риск.

